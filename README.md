# git-practice

This is a demo `git-practice` repository.

## Python GitHub Action Demo

This repository now includes:

- A basic Python program: `hello.py`
- A GitHub Actions workflow: `.github/workflows/python-demo.yml`

The workflow runs on pushes and pull requests to `main` (and can also be triggered manually), sets up Python 3.11, and executes the demo script.
